{
  "name": "world-anchor-ar-poc",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Runtime WebXR surface placement and native world anchors with Three.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node tests/world-browser-qa.cjs"
  }
}
