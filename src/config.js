export const CONFIG = Object.freeze({
  experienceUrl: './assets/experience.json',
  maxTrackingDimension: 640,
  detectIntervalMs: 1000 / 15,
  minSelectionPixels: 36,
  minTargetPixels: 20,
  revealFrames: 2,
  recoveryWindowMs: 3000,
  holdLastPoseMs: 220,
  staleAfterMs: 4500,
  minScore: 0.80,
});
